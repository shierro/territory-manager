version: "2"
plugins:
  duplication:
    enabled: true
    config:
      languages:
        - javascript
  eslint:
    enabled: true
    config:
      config: .eslintrc.js

exclude_patterns:
  - node_modules/
  - build/
  - coverage/
  - internals/
  - server/
