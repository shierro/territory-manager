// import React from 'react';
// import { shallow } from 'enzyme';

import LeftDrawer from '../index';

describe.skip('<LeftDrawer />', () => {
  it('Expect to have unit tests specified', () => {
    expect(LeftDrawer.length).toEqual(0);
  });
});
