const styles = theme => ({
  reCenterButton: {
    position: 'absolute',
    zIndex: 10000,
    bottom: 0,
    color: theme.palette.primary.dark,
  },
  addPersonButton: {
    color: theme.palette.primary.dark,
    position: 'absolute',
    zIndex: 10000,
    bottom: 10,
    right: 10,
  },
});

export default styles;
